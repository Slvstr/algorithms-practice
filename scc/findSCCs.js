(function() {
  'use strict';

  var graph = require('./graph').Graph;
  var reversedGraph = require('./graph').ReversedGraph;

console.log(Object.keys(graph).length);
console.log(Object.keys(reversedGraph).length);







})();